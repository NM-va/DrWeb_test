# Base project structure

## Stack
- HTML
- SCSS (mobile-first)
- JavaScript (vanilla)

## Start
1. Install dependencies:
   `npm install`
2. Run SCSS watcher:
   `npm run scss:watch`

## Build SCSS
`npm run scss:build`
